# Define the angular application.
# Inject the Bootstrap UI for carousel features.
# Inject interval for clockwork events such as apology expiration.
@app = angular.module "status-page", ["interval"]