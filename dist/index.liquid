<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        <!-- Refresh the page every few seconds. -->
        <meta http-equiv="refresh" content="20" />

        <!-- We don't want these pages indexed by Google until end of BETA. -->
        <meta name="robots" content="noindex, nofollow" />

        <!-- Dynamicly set the title using the helper methoe. -->
        <title>{{ page.name }}</title>

        <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
            <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
            <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
        <![endif]-->

        <!-- Include CSS Assets. -->
        <link rel="stylesheet" href="{{ 'stylesheets/status-page.css' | asset_url }}" />
    </head>
    <body>
        <!-- Header Block For Large Status Messages -->
        <header class="page-title state-{{ page.state }}">
            <div class="container">
                <!-- Output the name of this status page. -->
                <h1>{{ page.name }}</h1>
            </div>
        </header>

        <section class="current-state state-{{ page.state }}">
            <!-- Display current apologies if we have any. -->
            {% if page.current_apologies.length %}
                <!-- We have some apologies. -->
                <div class="apology-list">
                    <!-- Loop over the collection of current apologies. -->
                    {% for apology in page.current_apologies %}
                        <div class="apology">
                            <div class="container">
                                <!-- Output the title for the apology. -->
                                <h2>
                                    <!-- Output the date the apology was created, with live timer increment. -->
                                    <span class="block-time">Started 
                                        <time>
                                            {{ apology.created_at | time_ago_in_words }}
                                        </time> ago.
                                    </span>

                                    <!-- Output the apology description. -->
                                    {{ apology.description }}
                                </h2>

                                {% if apology.updates.length %}
                                    <!-- Loop over any updates we have for the apology, only displaying the last. -->
                                    {% for update in apology.updates %}
                                        <p class="update">
                                            <span class="block-time">
                                                Last Updated 
                                                <!-- Output the datetime for the update, with live timer incrememnt. -->
                                                <time>
                                                    {{ update.created_at | time_ago_in_words }} ago.
                                                </time>
                                            </span>

                                            <!-- OUtput the text content for the update. -->
                                            {{ update.content }}
                                        </p>
                                    {% endfor %}
                                {% else %}
                                    <!-- If we have no updates to display, show a message to this effect. -->
                                    <p class="no-updates">We have no further updates on this yet.</p>
                                {% endif %}
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% else %}
                <!-- No current apologies found -->
                <!-- We have nothing to be sorry about, so all is well. -->
                <h2>All Is Well</h2>
            {% endif %}

            <!-- Check if we have any previous apologies to display. -->
            {% if page.previous_apologies.length %}
                <!-- Anchor link down to previous apologies. -->
                <a href="#previous-apologies" class="see-below">&#65088;</a>
            {% endif %}            
        </section>

        <!-- Check if we have any previous apologies to display. -->
        {% if page.previous_apologies.length %}
            <!-- Block For previous apolgies. -->
            <section id="previous-apologies" class="previous-apologies">
                <div class="container">
                    <div class="apology-list previous-apology-list">
                        <h3>Recent Apologies</h3>

                        <!-- Loop over the collection of previous apologies, order date descending. -->
                        {% for apology in page.previous_apologies %}
                            <article class="apology previous-apology">
                                <h4>
                                    <!-- Output the date for the apology, with live timer increment. -->
                                    <span class="block-time">Started 
                                        <time>
                                            {{ apology.created_at | date: '%A %B %d %Y %I:%M %p' }}
                                        </time>
                                    </span>
                                    
                                    <!-- Output the description of the apology. -->
                                    {{ apology.description }}
                                </h4>

                                <dl class="dl-horizontal">
                                    <!-- Loop over the updates, showing all, in date order. -->
                                    {% for update in apology.updates %}
                                        <!-- Output the date for the update. -->
                                        <dt class="text-muted">{{ update.created_at | distance_of_time_in_words: apology.created_at, update.created_at }} in.</dt>
                                        <!-- Output the text content of the update. -->
                                        <dd>{{ update.content }}</dd>
                                    {% endfor %}
                                </dl>
                            </article>
                        {% endfor %}
                    </div>
                </div>
            </section>
        {% endif %}

        <!-- Include the required assets. -->
        <!-- Frameworks. -->
        <script src="{{ 'javascripts/status-page.js' | asset_url }}"></script>
    </body>
</html>